import countriesConfig from './countries.config';
import countriesOperations from './countries.operations'
import countriesServers from './countries.server'

export const countries = {
	config: countriesConfig,
	operations: countriesOperations,
	server: countriesServers
};
